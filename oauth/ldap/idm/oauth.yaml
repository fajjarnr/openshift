apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
    - ldap:
        attributes:
          email:
            - mail
          id:
            - dn
          name:
            - cn
          preferredUsername:
            - uid
        bindDN: 'uid=admin,cn=users,cn=accounts,dc=ocp,dc=fajjjar,dc=my,dc=id'
        bindPassword:
          name: idm-bind-password
        ca:
          name: idm-ca
        insecure: false
        url: 'ldaps://idm.ocp.fajjjar.my.id/cn=users,cn=accounts,dc=ocp,dc=fajjjar,dc=my,dc=id?uid'
      mappingMethod: claim
      name: Red Hat Identity Management
      type: LDAP
